## Профилирование данных

## Быстрый старт

```python
from src.data.load_data import load_raw_data
from src.data.data_profiler import check_data

# Загрузить данные
trips = load_raw_data(year=2024, month=1)

# Полный анализ
check_data(trips)
```

## Что анализируется

### 1. Базовая информация
- Размер датасета (строки, колонки)
- Размер в памяти
- Типы данных
- Список всех колонок

### 2. Анализ пропусков
- Общее количество и доля пропусков
- Строки с пропусками
- Детальная статистика по каждой колонке
- Цветовая индикация (красный > 50%, желтый > 10%, зеленый < 10%)

### 3. Проверка дубликатов
- Полные дубликаты строк
- Количество уникальных строк
- Доля уникальности

### 4. Числовые данные
- Статистика (count, mean, std, min, max, квартили)
- Для всех числовых колонок

### 5. Категориальные данные
- Количество уникальных значений
- Топ-5 самых частых значений с процентами
- Для всех строковых колонок

### 6. Временные данные
- Минимум и максимум
- Диапазон (для datetime)
- Пропуски

### 7. Примеры данных
- Первые N строк
- Последние N строк
- Случайная выборка

## Способы использования

### Способ 1: Полный отчет (рекомендуется)

```python
from src.data.data_profiler import check_data

check_data(df)
```

### Способ 2: Быстрая проверка (без примеров)

```python
from src.data.data_profiler import quick_check

quick_check(df)
```

### Способ 3: Детальный контроль

```python
from src.data.data_profiler import DataProfiler

profiler = DataProfiler(df)

# Вызываем только нужные методы
profiler.basic_info()
profiler.check_missing()
profiler.describe_categorical()
```

## Примеры

### Пример 1: Анализ загруженных данных

```python
from src.data.load_data import load_raw_data
from src.data.data_profiler import check_data

# Загрузить и проанализировать
trips = load_raw_data(year=2024, month=1)
check_data(trips)
```

### Пример 2: Сравнение до и после очистки

```python
import polars as pl
from src.data.data_profiler import quick_check

# До очистки
print("ДО ОЧИСТКИ:")
quick_check(trips)

# Очистка
trips_clean = trips.filter(
    pl.col('start_station_name').is_not_null()
)

# После очистки
print("\nПОСЛЕ ОЧИСТКИ:")
quick_check(trips_clean)
```

### Пример 3: Анализ конкретных аспектов

```python
from src.data.data_profiler import DataProfiler

profiler = DataProfiler(trips)

# Только пропуски
profiler.check_missing()

# Только категориальные данные
profiler.describe_categorical()

# Только временные данные
profiler.describe_datetime()
```

### Пример 4: Настройка вывода

```python
# Полный отчет с большими примерами
check_data(df, show_samples=True, sample_size=10)

# Без примеров данных
check_data(df, show_samples=False)

# Только базовая информация
quick_check(df)
```

## Цветовой вывод

Функция использует ANSI цвета для красивого вывода:

- 🔵 **Синий** - заголовки секций
- 🟢 **Зеленый** - хорошие показатели (нет пропусков, нет дубликатов)
- 🟡 **Желтый** - предупреждения (10-50% пропусков)
- 🔴 **Красный** - проблемы (>50% пропусков)
- **Жирный** - важная информация


## API Reference

### `check_data(df, show_samples=True, sample_size=3)`

Полный анализ данных.

**Параметры:**
- `df` (pl.DataFrame): DataFrame для анализа
- `show_samples` (bool): Показывать ли примеры данных
- `sample_size` (int): Количество строк в примерах

### `quick_check(df)`

Быстрая проверка (базовая информация, пропуски, дубликаты).

**Параметры:**
- `df` (pl.DataFrame): DataFrame для анализа

### `DataProfiler(df)`

Класс для детального контроля анализа.

**Методы:**
- `basic_info()` - базовая информация
- `check_missing()` - анализ пропусков
- `check_duplicates()` - проверка дубликатов
- `describe_numeric()` - числовые данные
- `describe_categorical()` - категориальные данные
- `describe_datetime()` - временные данные
- `sample_data(n)` - примеры данных
- `full_report()` - полный отчет

## Советы

1. **Используйте `check_data()` для первого знакомства с данными**
   ```python
   check_data(df)
   ```

2. **Используйте `quick_check()` для быстрой проверки**
   ```python
   quick_check(df)
   ```

3. **Используйте `DataProfiler` для детального анализа**
   ```python
   profiler = DataProfiler(df)
   profiler.check_missing()
   ```

4. **Сравнивайте данные до и после обработки**
   ```python
   print("ДО:")
   quick_check(df_raw)
   print("\nПОСЛЕ:")
   quick_check(df_clean)
   ```


## Jupyter Notebook

Смотрите полный пример в `notebooks/data_profiling_example.ipynb`

## Тестирование

```bash
python tests/test_profiler.py
```
